import React from "react";
import { shallow } from "enzyme";
import { GifExpertApp } from "../../components/GifExpertApp";

describe("Test en el archivo <GifExpertApp />", () => {
  test("Debe devolver un snapshot correctamente", () => {
//     const wrapper = shallow(<GifExpertApp />);

//     expect(wrapper).toMatchSnapshot();
//   });

//   test("Debe la  cantidad de defaultCategories", () => {

    // const category = ["un show mas", "spiderman"];
    // const wrapper = shallow(<GifExpertApp category={category}/>);

    // expect( wrapper.find('GifsGrid').length ).toBe( category.length );
  });
});